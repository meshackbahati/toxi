services:
  app:
    build: .
    image: oxidite-example:dev
    ports:
      - "8080:8080"
    environment:
      RUST_LOG: info
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8080/"]
      interval: 15s
      timeout: 3s
      retries: 5
